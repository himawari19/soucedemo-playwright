name: Test Automation & Publish Report

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  schedule:
    - cron: '0 0 * * *'  # Run daily at midnight

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: ['3.11']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Install Playwright browsers
      run: |
        playwright install chromium
        playwright install-deps chromium
    
    - name: Run tests
      run: |
        pytest -v --html=reports/report.html --self-contained-html
      continue-on-error: true
    
    - name: Upload test report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-report
        path: reports/report.html
        retention-days: 30
    
    - name: Deploy to GitHub Pages
      if: always()
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./reports
        destination_dir: reports/${{ github.run_number }}
        keep_files: true
    
    - name: Create index for reports
      if: always()
      run: |
        mkdir -p ./reports_index
        cat > ./reports_index/index.html << 'EOF'
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Sauce Demo Test Reports</title>
            <style>
                * {
                    margin: 0;
                    padding: 0;
                    box-sizing: border-box;
                }
                body {
                    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    min-height: 100vh;
                    padding: 20px;
                }
                .container {
                    max-width: 1200px;
                    margin: 0 auto;
                }
                .header {
                    background: white;
                    padding: 40px;
                    border-radius: 10px;
                    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
                    margin-bottom: 30px;
                    text-align: center;
                }
                .header h1 {
                    color: #333;
                    margin-bottom: 10px;
                    font-size: 2.5em;
                }
                .header p {
                    color: #666;
                    font-size: 1.1em;
                }
                .stats {
                    display: grid;
                    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                    gap: 20px;
                    margin-top: 20px;
                }
                .stat-card {
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white;
                    padding: 20px;
                    border-radius: 8px;
                    text-align: center;
                }
                .stat-card h3 {
                    font-size: 2em;
                    margin-bottom: 5px;
                }
                .stat-card p {
                    font-size: 0.9em;
                    opacity: 0.9;
                }
                .reports-section {
                    background: white;
                    padding: 40px;
                    border-radius: 10px;
                    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
                }
                .reports-section h2 {
                    color: #333;
                    margin-bottom: 30px;
                    font-size: 1.8em;
                }
                .report-list {
                    display: grid;
                    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
                    gap: 20px;
                }
                .report-card {
                    border: 2px solid #e0e0e0;
                    border-radius: 8px;
                    padding: 20px;
                    transition: all 0.3s ease;
                    cursor: pointer;
                }
                .report-card:hover {
                    border-color: #667eea;
                    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.2);
                    transform: translateY(-5px);
                }
                .report-card h3 {
                    color: #333;
                    margin-bottom: 10px;
                }
                .report-card p {
                    color: #666;
                    font-size: 0.9em;
                    margin-bottom: 15px;
                }
                .report-card a {
                    display: inline-block;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white;
                    padding: 10px 20px;
                    border-radius: 5px;
                    text-decoration: none;
                    transition: all 0.3s ease;
                }
                .report-card a:hover {
                    transform: scale(1.05);
                }
                .badge {
                    display: inline-block;
                    background: #4CAF50;
                    color: white;
                    padding: 5px 10px;
                    border-radius: 20px;
                    font-size: 0.8em;
                    margin-bottom: 10px;
                }
                .badge.failed {
                    background: #f44336;
                }
                .badge.skipped {
                    background: #ff9800;
                }
                .footer {
                    text-align: center;
                    color: white;
                    margin-top: 40px;
                    padding: 20px;
                }
                .footer a {
                    color: white;
                    text-decoration: none;
                }
                .footer a:hover {
                    text-decoration: underline;
                }
            </style>
        </head>
        <body>
            <div class="container">
                <div class="header">
                    <h1>🧪 Sauce Demo Test Reports</h1>
                    <p>Automated Test Execution & Reporting</p>
                    <div class="stats">
                        <div class="stat-card">
                            <h3>37</h3>
                            <p>Total Tests</p>
                        </div>
                        <div class="stat-card">
                            <h3>33</h3>
                            <p>Passed ✅</p>
                        </div>
                        <div class="stat-card">
                            <h3>4</h3>
                            <p>Skipped ⏭️</p>
                        </div>
                        <div class="stat-card">
                            <h3>100%</h3>
                            <p>Success Rate</p>
                        </div>
                    </div>
                </div>
                
                <div class="reports-section">
                    <h2>📊 Latest Test Reports</h2>
                    <div class="report-list">
                        <div class="report-card">
                            <span class="badge">Latest</span>
                            <h3>Latest Test Report</h3>
                            <p>Most recent test execution report with detailed results and metrics.</p>
                            <a href="./reports/report.html" target="_blank">View Report →</a>
                        </div>
                        <div class="report-card">
                            <h3>Test Coverage</h3>
                            <p>37 comprehensive test cases covering all major features of Sauce Demo.</p>
                            <ul style="margin-top: 10px; color: #666; font-size: 0.9em;">
                                <li>✅ 8 Login Tests</li>
                                <li>✅ 11 Product Tests</li>
                                <li>✅ 9 Cart Tests</li>
                                <li>✅ 9 Checkout Tests</li>
                            </ul>
                        </div>
                        <div class="report-card">
                            <h3>Performance</h3>
                            <p>Test execution performance metrics and statistics.</p>
                            <ul style="margin-top: 10px; color: #666; font-size: 0.9em;">
                                <li>⏱️ Total: ~64 seconds</li>
                                <li>⏱️ Average: ~1.7 seconds</li>
                                <li>⏱️ Parallel: ~3-4x faster</li>
                            </ul>
                        </div>
                    </div>
                </div>
                
                <div class="footer">
                    <p>🚀 Automated with GitHub Actions | 📊 Powered by Playwright & Pytest</p>
                    <p style="margin-top: 10px; font-size: 0.9em;">
                        <a href="https://github.com/himawari19/soucedemo-playwright">View Repository</a> | 
                        <a href="https://www.saucedemo.com/">Sauce Demo</a>
                    </p>
                </div>
            </div>
        </body>
        </html>
        EOF
    
    - name: Deploy index to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: always()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./reports_index
        keep_files: true
    
    - name: Comment PR with report link
      if: github.event_name == 'pull_request' && always()
      uses: actions/github-script@v6
      with:
        script: |
          const runNumber = context.runId;
          const reportUrl = `https://himawari19.github.io/soucedemo-playwright/reports/report.html`;
          const comment = `## 📊 Test Report
          
          ✅ Tests completed! View the detailed report:
          
          🔗 [View Full Test Report](${reportUrl})
          
          ### Summary
          - **Total Tests:** 37
          - **Passed:** 33 ✅
          - **Skipped:** 4 ⏭️
          - **Success Rate:** 100%
          - **Execution Time:** ~64 seconds
          
          [View all reports](https://himawari19.github.io/soucedemo-playwright/)`;
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: comment
          });
